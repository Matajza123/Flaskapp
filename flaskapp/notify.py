import datetime
from datetime import time

from flask_login import current_user
from flaskapp import db
from flask_sqlalchemy import BaseQuery

def check():
    while true:
        notify = Notify.order_by(notify_id).all()
        for event in nofity:
            print(event)

        sleep(500):

def push(id):
    pass
    